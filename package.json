{
  "name": "aws-object-signing-sharer",
  "version": "0.0.0",
  "description": "The root package of a mono repo for Elsa Data AWS object signing sharer",
  "private": true,
  "packageManager": "pnpm@8.7.0",
  "scripts": {
    "build": "pnpm --stream --filter \"./packages/**\" run build",
    "watch": "pnpm --stream --filter \"./packages/**\" run build:watch",
    "package": "pnpm --stream --filter \"./packages/**\" run package",
    "clean": "git clean -fXd"
  },
  "keywords": [],
  "author": {
    "name": "University of Melbourne Centre for Cancer Research",
    "organization": true
  },
  "license": "MIT",
  "devDependencies": {
    "esbuild": "0.25.0",
    "prettier": "3.0.3"
  }
}
